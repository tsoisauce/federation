# Python

Python is a user back-end service.

# Installation

- migrate database: `python manage.py migrate`
- load data: `python manage.py loaddata data.json`
- run server: `python manage.py runserver`
